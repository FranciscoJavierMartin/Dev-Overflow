meta {
  name: Increment views
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/questions/:id
  body: none
  auth: inherit
}

params:path {
  id: {{questionId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
